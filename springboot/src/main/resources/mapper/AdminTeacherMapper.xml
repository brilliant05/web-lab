<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boda.springboot.mapper.AdminTeacherMapper">

    <!-- 更新教师信息 -->
    <update id="updateTeacher" parameterType="com.boda.springboot.entity.User">
        UPDATE user
        <set>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="realName != null and realName != ''">real_name = #{realName},</if>
            <if test="email != null and email != ''">email = #{email},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="avatarUrl != null and avatarUrl != ''">avatar_url = #{avatarUrl},</if>
            <if test="status != null">status = #{status},</if>
            <if test="jobTitle != null and jobTitle != ''">job_title = #{jobTitle},</if>
            <if test="profile != null and profile != ''">profile = #{profile},</if>
            <if test="isDeleted != null">is_deleted = #{isDeleted},</if>
            update_time = NOW()
        </set>
        WHERE user_id = #{userId}
    </update>

    <!-- 分页查询教师 -->
    <select id="pageQuery" resultType="com.boda.springboot.entity.User">
        SELECT
            user_id,
            username,
            real_name,
            phone,
            avatar_url,
            status,
            job_title,
            profile,
            create_time,
            update_time
        FROM user
        WHERE role = 'TEACHER'
        AND is_deleted = 0
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="jobTitle != null and jobTitle != ''">
            AND job_title = #{jobTitle}
        </if>
        <if test="realName != null and realName != ''">
            AND real_name LIKE CONCAT('%', #{realName}, '%')
        </if>
        ORDER BY create_time DESC
    </select>

</mapper>