<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.boda.springboot.mapper.AnswerLikeMapper">

    <!-- 添加点赞 -->
    <insert id="save" parameterType="com.boda.springboot.entity.AnswerLike">
        INSERT INTO answer_like (answer_id, user_id, create_time)
        VALUES (#{answerId}, #{userId}, NOW())
    </insert>

    <!-- 取消点赞 -->
    <delete id="delete">
        DELETE FROM answer_like
        WHERE answer_id = #{answerId} AND user_id = #{userId}
    </delete>

    <!-- 查询是否已点赞 -->
    <select id="selectByAnswerAndUser" resultType="com.boda.springboot.entity.AnswerLike">
        SELECT * FROM answer_like
        WHERE answer_id = #{answerId} AND user_id = #{userId}
    </select>

    <!-- 批量查询点赞状态 -->
    <select id="selectLikedAnswerIds" resultType="java.lang.Long">
        SELECT answer_id FROM answer_like
        WHERE user_id = #{userId}
        AND answer_id IN
        <foreach collection="answerIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>

</mapper>

