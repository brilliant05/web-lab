<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boda.springboot.mapper.AdminStudentMapper">

    <!-- 更新学生信息 - 动态更新，只更新非空字段 -->
    <update id="updateStudent" parameterType="com.boda.springboot.entity.User">
        UPDATE user
        <set>
            <if test="username != null and username != ''">username = #{username},</if>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="realName != null and realName != ''">real_name = #{realName},</if>
            <if test="email != null and email != ''">email = #{email},</if>
            <if test="phone != null and phone != ''">phone = #{phone},</if>
            <if test="avatarUrl != null">avatar_url = #{avatarUrl},</if>
            <if test="role != null and role != ''">role = #{role},</if>
            <if test="status != null">status = #{status},</if>
            <if test="studentId != null and studentId != ''">student_id = #{studentId},</if>
            <if test="college != null and college != ''">college = #{college},</if>
            <if test="profile != null">profile = #{profile},</if>
            <if test="isDeleted != null">is_deleted = #{isDeleted},</if>
            update_time = NOW()
        </set>
        WHERE user_id = #{userId}
        AND is_deleted = 0
    </update>

    <!-- 分页查询学生 - 支持多条件筛选 -->
    <select id="pageQuery" resultType="com.boda.springboot.entity.User">
        SELECT
            user_id,
            username,
            real_name,
            email,
            phone,
            avatar_url,
            role,
            status,
            student_id,
            college,
            profile,
            create_time,
            update_time
        FROM user
        WHERE role = 'STUDENT'
        AND is_deleted = 0
        <if test="status != null">
            AND status = #{status}
        </if>
        <if test="college != null and college != ''">
            AND college LIKE CONCAT('%', #{college}, '%')
        </if>
        <if test="keyword != null and keyword != ''">
            AND (
                real_name LIKE CONCAT('%', #{keyword}, '%')
                OR student_id LIKE CONCAT('%', #{keyword}, '%')
                OR username LIKE CONCAT('%', #{keyword}, '%')
            )
        </if>
        ORDER BY create_time DESC
    </select>

    <!-- 根据ID查询学生详情 -->
    <select id="getById" resultType="com.boda.springboot.entity.User">
        SELECT
            user_id,
            username,
            real_name,
            email,
            phone,
            avatar_url,
            role,
            status,
            student_id,
            college,
            profile,
            create_time,
            update_time,
            is_deleted
        FROM user
        WHERE user_id = #{studentId}
        AND is_deleted = 0
    </select>

</mapper>

